SELECT d.name AS Department, e.name AS Employee, e.salary AS Salary FROM (SELECT id, NAME, Salary, DepartmentId, DENSE_RANK() OVER(PARTITION BY DepartMentId ORDER BY Salary DESC) AS Rank FROM Employee) e JOIN Department d ON e.DepartmentId = d.Id WHERE e.Rank = 1